---
layout: post
title: "[]"
subtitle: 
author: "Dongbo"
header-style: text
mathjax: true
hidden: false
tags:
  - 
---

2006 mcts 原始论文

abstract

蒙特卡洛评估？

serve as evaluation function at the leaves of min-max tree？
// 是啥？


1 Intro


2 Algorithm Structure

...

在选择节点时，如果节点被选择数量少于某个阈值，则通过启发式方法进行选择；

超过后，节点称为 internal node，此时将从value更高的节点中进行；

这样节点的选择策略将会逐渐变为 best-first 

3 Selectivity

在此之前的 Bouzy [9, 7]
// Move pruning techniques for Monte-Carlo Go
// Monte Carlo Go developments

认为 N 个随机变量  ... 均值 将满足 正态分布；因此将概率小于某个阈值的节点直接 剪掉，不再搜索

但完全不搜索，是有风险的；有些初始（因随机模拟导致的）比较差的节点，其中可能包含一些收益非常高的子节点。
因此不能将较差的节点探索概率完全将为0

这里的方法有：多臂赌博机 和 离散随机优化（discrete stochastic optimization）

这两种方法 假设概率分布是固定的，然而实际在 树搜索中 这不一定成立。
多臂赌博机中，探索的目标是最小化非最优解的探索次数，

这与 MCTS 的目标不同（MCTS允许非最优解被多次探索，只要最后选择的是最优解


// 随机优化不是很懂。


3.2 Crazy Stone‘a Algorithm



4 Backup Method

这里采用的 backup （backpropagation? 
是类似TD

// TD 算法
Learning to predict by the methods of temporal differences

4.1 Value Backup


4.2 Uncertainty Backup